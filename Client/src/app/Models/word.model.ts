export class WordModel{

    public constructor(public wordId?:number,public songId?:number, public wordValue?: string, public wordLength?: number,
        public wordCount?: number ){
    }
    
    }
